# dotfiles

Personal shell config. Symlinked into `$HOME`.

## Setup new machine

```bash
git clone git@github.com:dungxtd/dotfiles.git ~/dotfiles
ln -sf ~/dotfiles/zshrc ~/.zshrc
source ~/.zshrc
```

## Files

- `zshrc` → `~/.zshrc`
